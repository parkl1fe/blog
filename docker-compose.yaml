version: '3.3'

services:
  blog-db:
    image: mysql:8.0.22
    ports:
      - "3333:3306"
    environment:
       MYSQL_ROOT_PASSWORD: rootroot
       MYSQL_DATABASE: blogdb
       MYSQL_USER: mysql
       MYSQL_PASSWORD: pass
